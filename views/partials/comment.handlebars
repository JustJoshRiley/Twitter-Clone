<div class="Comment-Indent">
    <div class="Comment">
        <div class="DeletePostContainer">
            <a href="/comment/{{comment._id}}/edit/{{postId}}">✍️</a>
            <a href="/comment/{{comment._id}}/delete/{{postId}}">🗑</a>
        </div>
        <h2>t/{{comment.author.username}}</h2>
        <p>{{comment.content}}</p>
        <div class="Reply">
            <a class="reply" href="/posts/{{postId}}/comments/{{comment._id}}/replies/new">Reply</a>
        </div>
    </div>
    {{#each comment.comments}}
        {{> comment comment=this postId=../postId}}
    {{/each}}
</div>